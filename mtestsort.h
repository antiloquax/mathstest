/* mtestsort.h */

#ifndef _MTESTSORT_H_
#define _MTESETSORT_H_

/* Struct comparison functions from mtestsort.c. */
int stringcmp(const void *s, const void *t);
int intcmp(const void *a, const void *b);
int floatcmp(const void *a, const void *b);

#endif

